{
    "$schema": "./turbo.schema.json",
    "pipeline": {
        "build": {
            "inputs": [
                "src/**"
            ],
            "outputs": [
                "lib/**"
            ]
        }
    }
}