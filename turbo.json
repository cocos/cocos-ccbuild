{
    "$schema": "./turbo.schema.json",
    "pipeline": {
        "build": {
            "dependsOn": [],
            "inputs": [
                "src/**"
            ],
            "outputs": [
                "lib/**"
            ]
        }
    }
}