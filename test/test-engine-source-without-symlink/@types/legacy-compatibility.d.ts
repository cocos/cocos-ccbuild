// we have no defined '@types/cc-ambient-types' module in this engine project
// so we fallback to the legacy resolve to keep the compatibility.
// we need to ensure that the engine build tools could still work 
// when engine has no '@types/cc-ambient-types' module.
export declare const a: number;